import { reactMarkdownMermaid } from './react-markdown-mermaid';

describe('reactMarkdownMermaid', () => {
  it('should work', () => {
    expect(reactMarkdownMermaid()).toEqual('react-markdown-mermaid');
  });
});
