import { applicationHabitTracking } from './application-habit-tracking';

describe('applicationHabitTracking', () => {
  it('should work', () => {
    expect(applicationHabitTracking()).toEqual('application-habit-tracking');
  });
});
